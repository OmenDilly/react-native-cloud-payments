///
/// CardCryptogramParams.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

import NitroModules

/**
 * Represents an instance of `CardCryptogramParams`, backed by a C++ struct.
 */
public typealias CardCryptogramParams = margelo.nitro.cloudpayments.CardCryptogramParams

public extension CardCryptogramParams {
  private typealias bridge = margelo.nitro.cloudpayments.bridge.swift

  /**
   * Create a new instance of `CardCryptogramParams`.
   */
  init(cardNumber: String, expDate: String, cvv: String) {
    self.init(std.string(cardNumber), std.string(expDate), std.string(cvv))
  }

  var cardNumber: String {
    @inline(__always)
    get {
      return String(self.__cardNumber)
    }
    @inline(__always)
    set {
      self.__cardNumber = std.string(newValue)
    }
  }
  
  var expDate: String {
    @inline(__always)
    get {
      return String(self.__expDate)
    }
    @inline(__always)
    set {
      self.__expDate = std.string(newValue)
    }
  }
  
  var cvv: String {
    @inline(__always)
    get {
      return String(self.__cvv)
    }
    @inline(__always)
    set {
      self.__cvv = std.string(newValue)
    }
  }
}
