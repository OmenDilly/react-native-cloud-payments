///
/// ChargeStatus.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::cloudpayments {

  /**
   * An enum which can be represented as a JavaScript union (ChargeStatus).
   */
  enum class ChargeStatus {
    COMPLETED      SWIFT_NAME(completed) = 0,
    AUTHORIZED      SWIFT_NAME(authorized) = 1,
    DECLINED      SWIFT_NAME(declined) = 2,
    THREEDS      SWIFT_NAME(threeds) = 3,
  } CLOSED_ENUM;

} // namespace margelo::nitro::cloudpayments

namespace margelo::nitro {

  using namespace margelo::nitro::cloudpayments;

  // C++ ChargeStatus <> JS ChargeStatus (union)
  template <>
  struct JSIConverter<ChargeStatus> final {
    static inline ChargeStatus fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("Completed"): return ChargeStatus::COMPLETED;
        case hashString("Authorized"): return ChargeStatus::AUTHORIZED;
        case hashString("Declined"): return ChargeStatus::DECLINED;
        case hashString("ThreeDS"): return ChargeStatus::THREEDS;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum ChargeStatus - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, ChargeStatus arg) {
      switch (arg) {
        case ChargeStatus::COMPLETED: return JSIConverter<std::string>::toJSI(runtime, "Completed");
        case ChargeStatus::AUTHORIZED: return JSIConverter<std::string>::toJSI(runtime, "Authorized");
        case ChargeStatus::DECLINED: return JSIConverter<std::string>::toJSI(runtime, "Declined");
        case ChargeStatus::THREEDS: return JSIConverter<std::string>::toJSI(runtime, "ThreeDS");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert ChargeStatus to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("Completed"):
        case hashString("Authorized"):
        case hashString("Declined"):
        case hashString("ThreeDS"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
